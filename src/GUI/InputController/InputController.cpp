//
// Created by pawel on 07.12.2020.
//

#include "InputController.h"
//
//void InputController::initLexer(const QString& input) {
//    std::istringstream stream = std::istringstream(input.toStdString());
//    std::shared_ptr<SourceInterface> source =  std::make_shared<StringStreamSource>(stream);
//    lexer->setScanner(source);
//}
